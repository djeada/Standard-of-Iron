#include "world_chunk.h"

// Stage 18 — WorldChunk is header-only data, but we provide a TU so
// future frustum-cull helpers and batching logic live in one place.

namespace Render::GL {} // namespace Render::GL
